<?php

/*
 * Package-owned API routes. Empty by design -- WP 0A item 11 forbids product
 * behaviour in this package. The file exists so the provider's loadRoutesFrom call
 * is real and the seam is proven for both route kinds.
 */

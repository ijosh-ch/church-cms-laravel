<?php

namespace Ifgf\ChurchOperations\Support;

/**
 * The class ChurchOperationsMarkerPolicy is registered against.
 *
 * Gate::policy() needs a class to key on. This exists solely so the policy
 * registration is real and assertable rather than pointing at a name that does not
 * resolve. No product meaning; delete with the marker policy.
 */
class MarkerResource
{
}
